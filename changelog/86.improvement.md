If a required attribute is missing during validation, we now raise the more explicit [`MissingAttributeError`][.input4mips_validation.validation.error_catching.MissingAttributeError], rather than a `KeyError`.
